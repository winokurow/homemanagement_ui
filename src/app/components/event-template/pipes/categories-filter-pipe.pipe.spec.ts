import { CategoriesFilterPipePipe } from './categories-filter-pipe.pipe';

describe('CategoriesFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
