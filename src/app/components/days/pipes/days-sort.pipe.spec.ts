import {DaysSortPipe} from "./days-sort.pipe";

describe('DaysFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSortPipe();
    expect(pipe).toBeTruthy();
  });
});
